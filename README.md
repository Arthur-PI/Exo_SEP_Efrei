# Exo_SEP_Efrei

## Start the DB with docker
`docker compose up -d`

## Go in the api folder
`cd api`

## Install packages
`pnpm install`

## Initialize DB
`pnpm prisma generate`
`pnpm prisma migrate dev`

## Run the app 😊
`pnpm start`
